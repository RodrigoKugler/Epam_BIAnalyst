# SQL Lab Assets

- `sample_sales_orders.csv`: Minimal dataset for KPI, ranking, and churn exercises described in the SQL module.
- Usage tips:
  - Load into staging table via `COPY` or `INSERT` statements for practice.
  - Extend with additional columns (e.g., `channel`, `region`) to explore segmentation.
